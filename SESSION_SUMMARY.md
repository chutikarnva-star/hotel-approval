# สรุปโปรเจกต์: ระบบขออนุมัติที่พัก (Hotel Approval)

วันที่: 2026-08-14
โฟลเดอร์โปรเจกต์: `C:\Users\chutikarn.va\Desktop\Hotel Approval`

## โจทย์

แอปสำหรับพนักงานที่ต้องไปทำงานสาขาไกลจาก Store Center (สาขาใกล้บ้านที่สุด) ใช้ขออนุมัติที่พัก
โดยข้อมูลหลังบ้านเดิมอยู่ใน Google Sheet:
https://docs.google.com/spreadsheets/d/18_MIyHiYXo8uqJSGyLsUOOsU0s4KjHKc0dHZY2qdHVg

## สแตกที่ใช้

- Next.js 14 (App Router, TypeScript)
- PostgreSQL + Prisma ORM
- Firebase Authentication (Google login, จำกัดโดเมน @cjmart.co.th)
- Deploy เป้าหมาย: Google Cloud Run ผ่าน Launchpad

## การตัดสินใจสำคัญ (ตอบคำถามที่ถามระหว่างทำ)

1. **เก็บข้อมูลที่ไหน**: ย้ายข้อมูลอ้างอิง (Master List โรงแรม, ผู้อนุมัติ, พนักงาน, สาขา) เข้าฐานข้อมูลของแอปเอง
   ไม่ได้ใช้ Google Sheet เป็นฐานข้อมูลสด — นำเข้าผ่านหน้า Admin ด้วยไฟล์ CSV
2. **Store Center ของพนักงาน**: ดึงอัตโนมัติจากตาราง Employee (ผูกกับ Branch) ไม่ต้องให้ผู้ใช้เลือกเอง
3. **แจ้งเตือนผู้อนุมัติ**: ตอนนี้แจ้งในแอปเท่านั้น (ผู้อนุมัติ login มาดูคิวเอง) ยังไม่ส่งอีเมลจริง
   เพราะอีเมลผู้อนุมัติในชีทยังไม่ครบ — มีหน้า `/admin/approvers` ให้กรอกอีเมลภายหลังได้

## โครงสร้างข้อมูลหลัก (Prisma schema: `prisma/schema.prisma`)

- **Branch** — สาขา (รหัส, ชื่อ, lat/lng, งบต่อคืน, สาขาใหม่หรือไม่, วันเปิดสาขา)
- **Employee** — พนักงาน (รหัส, ชื่อ, หน่วยงาน AM/RM/TN/Audit, Store Center, มีรถบริษัทหรือไม่, ผูกกับบัญชี login)
- **Hotel** — โรงแรมใน Master List (ผูกกับสาขา, ระยะทาง, ราคา, ระดับการแนะนำ)
- **Approver** — ผู้อนุมัติต่อหน่วยงาน (nickname, ชื่อเต็ม, อีเมล)
- **Request** — คำขอที่พัก (ครบทุกฟิลด์ตามแบบฟอร์มเดิมในชีท: ระยะทาง, ผลตรวจสิทธิ์, โรงแรมที่เลือก, ราคา/งบ/ส่วนต่าง, สถานะ, ผลอนุมัติ)

## Logic หลักที่ implement (`src/lib/`)

- `distance.ts` — คำนวณระยะทาง Store Center ↔ สาขาปลายทาง ด้วยสูตร Haversine
- `eligibility.ts` — ตรวจสิทธิ์ตาม flowchart: ระยะทาง (มีรถ >60กม. / ไม่มีรถ >15กม.) → ถ้าไม่ผ่าน เช็ค AM/TN ทีม 2 คนคนละกะ → AM เปิด 2 สาขาพร้อมกัน → เหตุผลอื่น
- `hotelLevel.ts` — สูตรระดับการแนะนำโรงแรม (≤500ม.และ≤560บาท=1, ≤560บาทไกลกว่า=2, เกิน560บาท=3)
- `statusFlag.ts` — สรุปสถานะ เขียว/เหลือง/แดง ให้ผู้อนุมัติ (ตามแนวคิดจากแท็บ "สรุปสถานะคำขอที่พัก" ในชีทเดิม)

## หน้าเว็บที่มี

| หน้า | ใช้งานโดย | คำอธิบาย |
|---|---|---|
| `/requests/new` | พนักงาน | ฟอร์มขอจองที่พัก (ทีละขั้นตอน) |
| `/requests` | พนักงาน | ดูคำขอของตัวเอง + สถานะ |
| `/approvals` | ผู้อนุมัติ | คิวรออนุมัติของหน่วยงานตัวเอง + สรุปตัวเลข |
| `/admin` และ `/admin/branches` `/admin/hotels` `/admin/employees` `/admin/approvers` | Admin (ตาม `ADMIN_EMAILS`) | นำเข้า/ดูข้อมูลอ้างอิงผ่าน CSV |

## ข้อสมมติที่ควรตรวจสอบภายหลัง

1. เกณฑ์ระยะทาง "หรือ 1 ชม." — ตอนนี้เช็คแค่ระยะทาง (กม.) ไม่ได้เช็คเวลาเดินทางจริง
2. สูตรระดับการแนะนำ — ทำตามคำอธิบายเป๊ะๆ แต่ข้อมูลจริงบางแถวในชีทไม่ตรงกับสูตร (อาจเป็นการแก้มือ)
3. หน้าต่างสาขาใหม่ (D-29 ในแท็บงบต่อคืน) — ยังไม่ได้ทำอัตโนมัติ ให้ผู้ใช้ตอบเองเหมือนเดิม
4. การนำเข้าข้อมูลจริงจากชีท (พนักงาน ~300 / สาขา ~200 / โรงแรม ~100) ยังไม่ได้ทำ — ต้อง export แต่ละแท็บเป็น CSV แล้วอัปโหลดผ่านหน้า Admin เอง (ลำดับ: สาขา → พนักงาน → โรงแรม → ผู้อนุมัติ)

## สถานะปัจจุบัน

- โค้ดเขียนเสร็จ, `npm install` แล้ว, `npm run build` ผ่าน (type-check สำเร็จ)
- `git init` + commit แรกเรียบร้อยแล้วในเครื่อง (ยังไม่ได้ push ขึ้น GitHub เพราะเครื่องนี้ไม่มี GitHub CLI (`gh`) ติดตั้ง)

## สิ่งที่ต้องทำต่อ

1. ติดตั้ง `gh` หรือสร้าง repo เองผ่านเว็บ github.com แล้วให้ผม push โค้ดขึ้น (ดูวิธีในบทสนทนา)
2. สร้าง Firebase project + เปิด Google sign-in + คัดลอกค่า config มาใส่ `.env.local` (ดู `.env.example` ว่าต้องใช้ตัวแปรอะไรบ้าง)
3. รันทดสอบในเครื่อง:
   ```bash
   cp .env.example .env.local
   npm run db:up
   npm run db:migrate:dev
   npm run dev
   ```
4. นำเข้าข้อมูลจริงผ่านหน้า `/admin` (CSV) ตามลำดับ: Branches → Employees → Hotels → Approvers
5. เมื่อทดสอบโอเคแล้ว รัน `/launchpad:deploy` เพื่อขอ Launchpad host ให้เป็นของจริง
